// Inicia o objeto que será exportado para uso em outros arquivos do projeto.
module.exports = {
  // Agrupa em VALIDATION as constantes usadas nas regras de validação.
  VALIDATION: {
    // Define o tamanho mínimo permitido para o nome de usuário.
    USERNAME_MIN: 3,
    // Define o tamanho máximo permitido para o nome de usuário.
    USERNAME_MAX: 20,
    // Define o tamanho mínimo permitido para a senha.
    PASSWORD_MIN: 6,
  // Fecha a estrutura iniciada nas linhas anteriores.
  },
// Fecha o bloco de código ou objeto atual.
}
