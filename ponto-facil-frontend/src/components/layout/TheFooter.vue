<!-- Linha original 1: Inicia a seção visual do componente Vue. -->
<template>
  <footer class="footer">
    <small>Ponto Fácil © 2026 — Projeto acadêmico</small>
  </footer>
</template>

<!-- Linha original 7: Inicia o bloco CSS do componente; scoped limita os estilos ao próprio componente quando presente. -->
<style scoped>
/* Linha original 8: Inicia o bloco de estilos aplicado ao seletor .footer. */
.footer {
  /* Linha original 9: Define o espaçamento interno com o valor 14px 28px. */
  padding: 14px 28px;
  /* Linha original 10: Define a cor do texto com o valor var(--text-muted). */
  color: var(--text-muted);
  /* Linha original 11: Define o fundo com o valor #fff. */
  background: #fff;
  /* Linha original 12: Configura a propriedade CSS border-top com o valor 1px solid var(--tertiary-color). */
  border-top: 1px solid var(--tertiary-color);
  /* Linha original 13: Define o alinhamento do texto com o valor center. */
  text-align: center;
/* Linha original 14: Encerra o bloco de regras CSS iniciado anteriormente. */
}
  /* Linha original 15: Encerra o bloco de estilos deste componente. */
</style>

<!--
GUIA LINHA A LINHA DO TEMPLATE
O template foi mantido sem comentários entre seus atributos para não alterar a sintaxe das tags Vue.
Linha original 1: <template>
  -> Inicia o template, seção que define a estrutura visual renderizada pelo componente Vue.
Linha original 2: <footer class="footer">
  -> Inicia o rodapé da aplicação.
Linha original 3: <small>Ponto Fácil © 2026 — Projeto acadêmico</small>
  -> Cria o elemento small com os atributos definidos nesta marcação.
Linha original 4: </footer>
  -> Encerra o elemento footer iniciado anteriormente.
Linha original 5: </template>
  -> Encerra o template do componente Vue.
-->
